first_step: init
last_step: done
default_help:
  - "For medication information, say: medication information. For track setup, say: setup tracker"

steps:
  init:
    events:
      launched:
        next: choose_module

  choose_module:
    reprompt:
      - "sorry I didn't understand that. Please select either: medication information. or. setup tracker"
    help:
      - "For medication information, say: medication information. To setup tracker, say: setup tracker"
    events:
      add_medication_to_plan:
        next: inputmeds

  inputmeds:
    reprompt:
      - "test"
    help:
      - "test"
    events:
      tracker_input:
        next: done

  done:
